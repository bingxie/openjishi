<% unless @profile.errors.empty? %>
  alert("<%= @profile.errors[:avatar][0] %>")
  $('#avatar_upload .saving-icon').hide();
<% else %>
  $('#avatar_upload').hide();
  $('#crop_image_form').html('<%= escape_javascript(render :partial => 'crop') %>');
  avatarCropper.init();
  formWithSaveIcons("#avatar_crop").init();
<% end %>
