<%- if resource.errors[:email].empty? %>
  $('input[type="submit"]').prop('value', "发送成功");
  $('input[type="submit"]').prop('disabled', true);
<%- else %>
  $('label.error.unregistered').show();
  $('#user_email').focus(function(){
    $('label.error.unregistered').hide();
  });
<%- end %>
