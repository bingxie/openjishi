<% if resource.errors.empty? %>
  show_done("#change_password");
<% else %>
  $('#change_password_form').empty().append("<%= j render('edit') %>");
  $('#change_password').enableClientSideValidations();

  init_form_with_save_icons("#change_password");
<% end %>
